# sample alpha

This file is a static fixture used by auto-refresh e2e tests.
Tests COPY this file into a tmpdir before opening it, so the on-disk
content can be mutated freely without dirtying the source tree.
